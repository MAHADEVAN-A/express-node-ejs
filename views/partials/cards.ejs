<div class="projects">
    <div class="container1">
        <% var count = count %>
        <% for(var i=1;i<=count;i++){ %>
        <div class="cards">
            <div class="imge">
                <img src="/assets/<%=image%>/<%=cont[i-1].image%>.svg"/>
            </div>
            <div class="content">
                <h2><%= cont[i-1].content %></h2>
                <button data-modal-target="#modal1<%=i%>">Edit</button>
                <button class="deletebtn">Delete</button>
                <div class="modal" id="modal1<%=i%>">
                    <div class="modal-header">
                        <div class="title">
                            Choose the file:
                        </div>
                        <button data-close-button class="close-button">
                            &times;
                        </button>
                    </div>
                    <div class="modal-body">
                        <form id="<%= title %><%=i%>" action="/api/<%= title %>/<%=i%>" method="POST" enctype="multipart/form-data">
                            <label for="content"><h2>Enter the content:</h2></label>
                            <input type="text" name="content">
                            <input type="file" name="image">
                            <button class="<%= title %><%=i%><%=i%>" form="<%= title %><%=i%>" type="submit">SEND</button>
                        </form>
                    </div>
                </div>
                <div id="overlay">
            
                </div>
            </div>
        </div>
        <% } %>
        
    </div>
</div>
<script>
    var cards = document.getElementsByClassName('imge');
    var butto = document.getElementsByClassName('deletebtn');
    console.log(cards);
     var count = '<%-count %>'
     var detail = '<%-detail%>'
     console.log(count,'maha');
     var detailid = '<%-detailid%>'
     const detaillid = detailid.split(',')
     console.log(detaillid)
     for(let j=0;j<count;j++){ 
         console.log(j,count,detaillid[j],'devan');
         let loca = `http://localhost:5000/${detail}/${detaillid[j]}`;
        cards[j].addEventListener('click',function(){
            window.location= loca;
            console.log(j,'count');
        })
        var dele = '<%- delet%>'
        
        butto[j].addEventListener('click',()=>{
         console.log(j,count,detaillid[j],'de');  
            window.location= `http://localhost:5000/api/${dele}/${detaillid[j]}`;
        })
     }

</script>